import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
  palette: {
    mode: "light",
    primary: { main: "#0a2cc5a9" },
    success: { main: "#459f00" },
    warning: { main: "#ef6c00" },
  },
});
